# collection:   netapp.ontap
# role:         na_ontap_nas_create

# Description:  NFS export example
nas:
    - { name: nfs_share, protocol: nfs, vserver: "{{ vserver }}", client: 192.168.7.0/24, ro: sys, rw: sys, su: sys, aggr: aggr1, size: 10 }

# Description:  CIFS share example
# nas:
    #- { name: cifs_share, protocol: cifs, vserver: cifs_vserver, aggr: aggr1, size: 10, share: cifs_share_1 }